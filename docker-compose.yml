version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ENV=production
    volumes:
      - ./app:/app/app
      - ./static:/app/static
    restart: unless-stopped
